import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata = {
  title: "Abdul Aziz | Front-End Developer",
  description: "Welcome to the portfolio of Abdul Aziz, a skilled front-end developer specializing in building responsive, high-performance websites with Next.js, Tailwind CSS, and JavaScript. With a focus on clean code, seamless functionality, and a user-centered design approach,",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
